
events {
        worker_connections 4096;
}

http {
        server {
                listen 80;
                listen [::]:80;
                server_name psymacha.org;
                root /var/www/html;
                index index.html index.htm index.nginx-debian.html;


        }
}